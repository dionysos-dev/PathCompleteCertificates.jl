[deps]
Aqua = "4c88cf16-eb10-579e-8560-4a9242c79595"
PathCompleteCertificates = "b391c707-6ba7-482f-97f4-86d517cfbcc9"
Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"

# Solvers live here rather than in the package: an artifact reviewer must be able
# to run everything with no licence, and the package itself stays solver-agnostic
# through MathOptInterface.
#   Clarabel — conic (LP/QP/SOCP/SDP), native Julia
#   HiGHS    — LP, for the polyhedral templates
# Added in the commit that first needs them.

[sources]
PathCompleteCertificates = {path = ".."}
